{
  "framework": "vite",
  "installCommand": "cd .. && npm install -g npm@latest && npm install --legacy-peer-deps && npm install @tailwindcss/oxide-linux-x64-gnu@4.3.3 --legacy-peer-deps --no-save",
  "buildCommand": "cd .. && npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
