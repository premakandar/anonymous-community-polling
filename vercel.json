{
  "framework": "vite",
  "installCommand": "npm install -g npm@latest && npm install --legacy-peer-deps && npm install @tailwindcss/oxide-linux-x64-gnu@4.3.3 --legacy-peer-deps --no-save",
  "buildCommand": "npm run build",
  "outputDirectory": "pulseboard-ui/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
